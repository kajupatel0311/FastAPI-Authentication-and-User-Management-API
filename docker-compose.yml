version: "3.9"

services:

  backend:

    build: .

    container_name: backend-project

    env_file:
      - .env

    ports:
      - "8000:8000"

    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs

    restart: unless-stopped